#include "iter.hpp"

template<typename T>
void fill_array(T * array)
{
    std::cout << 
}

int main(void)
{
    std::string tab[3] = {"lol", "test", "random"};
    iter(tab, 4, &fill_array);
    return (0);
}